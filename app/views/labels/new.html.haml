%main.inner.cover.text-dark.shadow-lg{:role => "main"}
  .row.justify-content-center
    .card
      .text-center
        = image_tag(@image.file, alt: "Imagem da pergunta", class: 'img-fluid', style: 'height:auto;width:600px;' )
        .card-body
          %h5.card-title
            = @question.description
        %ul.list-group.list-group-flush
          - @answer_opts.each do |answer_opt|
            %li.list-group-item
              %button.btn.btn-light
                -#= link_to answer_opt.text, controller: "labels", action: "create", id: answer_opt.id
                = link_to answer_opt.text
        .card-body
          %a.card-link{:href => "/label"} Pular