%main{:role => "main"}
  .album.py-5
    .container
      .row
        - @projects.each do |project|
          - @image = project.images.sample
          .col-md-4
            .card.mb-4.box-shadow
              = image_tag(@image.file, alt: "Imagem da pergunta", class: 'card-img-top')
              /%img.card-img-top{:alt => "Card image cap", "data-src" => @image.file}/
              .card-body.text-dark
                %p.card-text= project.name                
                %hr
                %p.card-text= project.description
                /%p.card-text= project.user_id
                .d-flex.justify-content-between.align-items-center
                  .btn-group
                    %button.btn.btn-sm.btn-outline-secondary{:type => "button"}= link_to "Recuperar dados", "#"
                    %button.btn.btn-sm.btn-outline-secondary{:type => "button"}= link_to "Ver mais", project_path(project)

                  /%small.text-muted 9 mins                  
              
